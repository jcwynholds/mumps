This is Jan W's extensions for mumps 1.60

planned extensions:

- packing and dollar ($P, $E) functions inlined assembler (nasm)
- btree data structure (iso linked list)
- libraries in c++ loaded by c runtime (error handling and stack control)
- configurable threaded worker model (posix threading)
- transactional memory iso sys/ipc.h semaphores (c++11)
- incremental thread-id locks
- transactional tuning support
- atoms stored as c++11 auto vars
- btree replication/sharding (both in memory and uci/datastore)
- mysql engine integration (innodb engine workalike)
